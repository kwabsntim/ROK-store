// ROK Store — Frontend Configuration

const ROK_CONFIG = {
    // Cloudinary — Settings → Upload → Upload Presets (create one, set to Unsigned)
    CLOUDINARY_CLOUD_NAME:    'ns69k7e5',
    CLOUDINARY_UPLOAD_PRESET: 'rok-store-preset-unsigned',

    // Backend API base URL
    API_BASE: 'https://rokskatesplug.onrender.com',
};

